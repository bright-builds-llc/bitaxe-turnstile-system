# 06: Reduce the owner-approved continuity sample to four cycles

**Status:** resolved
**Blocked by:** None

- [x] Record the explicit four-cycle owner decision and update the active protocol specification.
- [x] Preserve closed twenty-cycle records and unchanged deployed Gate `61cf6c5` browser/runtime contracts.
- [x] Verify and publish the documentation revision for the firmware-side qualification amendment.

## Scope

The owner changed only the repetition count from 20 to 4 after the first two completed cycles; cycles three and four subsequently passed. The firmware supervisor will require all four genuine reports and preserve its original context, package, browser artifact, campaign and private page baseline. Signed leases, privacy and the 240-second device ceiling remain unchanged. No browser or cryptographic implementation change is needed.

## Review

Format, Rust lint/build/tests, TypeScript and 299 web/CLI tests pass. The initial headless browser fixture did not publish its URL within the existing startup limit; after a successful explicit fixture prebuild, browser conformance, lookup vectors, package and standards checks pass with unchanged limits. This is documentation-only: the qualified Gate `61cf6c5` browser artifact and signing/runtime code remain unchanged. The firmware-side amendment will bind this documentation revision separately and preserve all four actual cycle reports and the original mining budget.
