# Keep terminal Protected Action outcomes immutable

Once a Protected Action Outcome reaches `succeeded` or `failed`, it cannot be reopened or rewritten. Operational corrections use separately auditable administrative records, and a new attempt requiring authorization is modeled as a genuinely new Protected Action with a new Action Reference.
