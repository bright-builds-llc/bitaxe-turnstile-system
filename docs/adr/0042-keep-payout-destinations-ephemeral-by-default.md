# Keep Payout Destinations ephemeral by default

Payout Destinations will be selected per challenge and will not silently persist, copy from identity profiles, appear in Gate Passes, or reach Relying Services. Clients may offer opt-in local storage and signed-in management address books with correlation warnings, while Pool Adapters receive only the payout instruction needed for job construction and users are encouraged to provide fresh wallet receive addresses.
