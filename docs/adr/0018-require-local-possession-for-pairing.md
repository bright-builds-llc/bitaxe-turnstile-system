# Require local possession for persistent pairing

Persistent pairing will require a local USB ceremony that authenticates a firmware-generated Device Identity and receives explicit confirmation before granting control. A QR code may transfer an opaque, expiring reference for that authenticated ceremony to another screen, but possession of the QR code, device serial number, or network address alone will not establish management authority or expose long-lived secrets.
