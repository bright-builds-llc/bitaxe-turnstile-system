# Measure normalized work rather than share count

A work challenge will complete after accumulating a required quantity of target-derived credited work, not a required number of accepted shares. Normalization makes completion independent of variable-difficulty configuration and worker size: results representing equivalent expected hashing effort receive equivalent credit even when one worker submits many easy shares and another submits fewer difficult shares.
