# Require Bitcoin-productive work

All work eligible to satisfy a proof-of-work gate will search valid Bitcoin block candidates, even though completion is measured using results below Bitcoin network difficulty. This adds complexity compared with hashing arbitrary challenge data, but avoids deliberately consuming energy on synthetic work that can never contribute to Bitcoin block discovery.
