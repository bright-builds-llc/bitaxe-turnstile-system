# Require reference firmware only for the automated Bitaxe flow

The polished Bitaxe Reference Client will require Bright Builds Reference Firmware so it can safely dispatch challenge-scoped work and restore ordinary mining, but the protocol will continue accepting other compatible Workers through an advanced configuration path. Firmware migration will preserve existing settings by default and verify retention after reboot, rather than treating a destructive factory reset as normal onboarding.
