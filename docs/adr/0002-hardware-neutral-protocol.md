# Keep the protocol hardware-neutral

The proof-of-work gating protocol will accept valid work from any compliant worker rather than requiring or attesting to Bitaxe hardware. Bright Builds will initially provide a Bitaxe-first reference client and onboarding experience, preserving a focused product path without coupling the interoperable protocol or its security model to one device family.
