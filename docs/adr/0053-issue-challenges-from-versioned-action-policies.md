# Issue challenges from versioned Action Policies

Relying Services will register audited, versioned Action Policies that fix work, Pool Offer, Reward Policy, lifecycle, fallback, branding, and safety constraints for each Protected Action type. Challenge requests select a revision and provide an opaque Action Reference plus only permitted bounded overrides; issued policy is immutable, so later administrator changes never alter active work.
