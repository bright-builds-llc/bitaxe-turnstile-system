# Use passwordless multi-authenticator accounts

Accounts will use stable internal Account Identities with one or more linked passkey, email-code, or Nostr NIP-46 Authenticators; no passwords or password hashes will be stored. Authenticator changes require Step-Up Authentication, account discovery will not disclose registration state, and separately existing identities will merge only through an explicit auditable ceremony rather than automatic credential matching.
