# Use explicit federated trust

A Relying Service will trust configured Gate Authority identities rather than independently validating every share and Bitcoin job. Each Gate Authority may in turn trust configured Pool Adapters for work accounting; signed records keep these relationships authenticated and auditable, while self-hosting lets an operator collapse the delegation boundary. The protocol will describe this as federated trust and will not claim globally trustless verification.
