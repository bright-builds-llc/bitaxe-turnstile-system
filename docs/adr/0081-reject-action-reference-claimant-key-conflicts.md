# Reject Action Reference Claimant-key conflicts

The Relying Service pins one Claimant-key thumbprint to an Action Reference when it creates the Protected Action. A Gate Pass bound to a different Claimant key is rejected as an integrity conflict without Pass Consumption and without revealing whether a Redemption Record or Protected Action Outcome already exists.
